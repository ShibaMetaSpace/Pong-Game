export default {
  /**
   * The width of the game (px).
   */
  width: 750,

  /**
   * The height of the game (px).
   */
  height: 480,

  /**
   * The ball diameter (px).
   */
  ballSize: 16,

  /**
   * The player's pad size (px).
   */
  padSize: 64,

  /**
   * The speed of the pad.
   */
  padSpeed: 1,

  /**
   * The interval between 2 frame updates (ms).
   * Increase the value to make the game slower.
   * Decrease the value to make the game faster.
   */
  tickInterval: 5,
};
